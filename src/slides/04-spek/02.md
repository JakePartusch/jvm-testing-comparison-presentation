### Behavior Driven Design
```kotlin
given("a calculator") {
    on("addition") {
        it("should return the sum") {
        }
    }
    on("subtraction") {
        it("should return the difference") {
        }
    }
}
```
